# 前后端全流程验证测试记录

## 文档目的

本文档用于记录 2026-04-10 对本项目进行的前后端全流程验证结果。

本次验证采用并行方式执行：

- 子代理 A：负责后端全流程验证
- 子代理 B：负责前端全流程验证

文档可作为赛题交付前的联调与验收自检材料。

## 测试时间

- 日期：2026-04-10

## 测试范围

### 后端范围

- Spring Boot 3 服务启动
- MySQL 连接与 Flyway 迁移
- 单元测试执行
- 登录接口可用性
- 基础权限与异常处理风险排查

### 前端范围

- Vue3 + Element Plus 项目依赖与构建
- Vite 本地开发服务启动
- `/api` 代理联通性
- 登录后关键页面访问与菜单可见性
- 前后端联调下的基础行为验证

## 测试环境

- 仓库目录：`/Users/dengfulei/Desktop/codex-ai/skill-admin`
- 测试日期：`2026-04-10`
- 后端目录：`backend`
- 前端目录：`frontend`
- 数据库：本地 MySQL
- 前端开发端口：`5173`
- 后端默认端口：`8080`
- 后端临时验证端口：`18080`

## 测试依据

- [README.md](/Users/dengfulei/Desktop/codex-ai/skill-admin/README.md)
- [接口与权限说明](/Users/dengfulei/Desktop/codex-ai/skill-admin/docs/接口与权限说明.md)
- [演示脚本](/Users/dengfulei/Desktop/codex-ai/skill-admin/docs/演示脚本.md)
- [交付检查表](/Users/dengfulei/Desktop/codex-ai/skill-admin/docs/交付检查表.md)

## 后端验证记录

### 已执行命令

```bash
mvn -v
java -version
cd backend && mvn test
lsof -iTCP:3306 -sTCP:LISTEN
mysql -uroot -p123456 -e "SHOW DATABASES LIKE 'skill_admin';"
mysql -uroot -p123456 skill_admin -e "SHOW TABLES;"
mysql -uroot -p123456 skill_admin -e "SELECT installed_rank, version, description, success FROM flyway_schema_history ORDER BY installed_rank;"
cd backend && mvn spring-boot:run
lsof -nP -iTCP:8080 -sTCP:LISTEN
curl -X POST http://localhost:8080/api/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"123456"}'
cd backend && mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=18080
```

### 通过项

- `mvn test` 通过，后端单测共 `9/9` 成功。
- 本地 MySQL 可连接，`skill_admin` 数据库存在。
- Flyway 迁移成功执行至 `V3`。
- 后端在 `18080` 端口可正常启动，说明 Spring Boot、JPA、Flyway、JWT 依赖链路可运行。
- 登录接口 `/api/auth/login` 返回成功，能正常获取 token。

### 阻塞项

- 默认命令 `mvn spring-boot:run` 使用 `8080` 启动时失败。
- 原因是本机已有 Java 进程占用 `8080` 端口，属于环境占用，不是代码启动失败。

### 风险项

#### 1. 业务异常的 HTTP 状态码语义不一致

- 位置：[GlobalExceptionHandler.java](/Users/dengfulei/Desktop/codex-ai/skill-admin/backend/src/main/java/com/codex/skilladmin/common/GlobalExceptionHandler.java#L14)
- 现状：`BusinessException` 被统一标记为 HTTP `400 BAD_REQUEST`
- 影响：即使业务内部希望表达 `401` 或 `403`，HTTP 响应层仍会固定返回 `400`
- 风险：权限拒绝、未授权等场景的状态码语义可能与验收预期不一致

## 前端验证记录

### 已执行命令

```bash
node -v
npm -v
cd frontend && npm ls --depth=0
cd frontend && npm run build
cd frontend && npm run dev -- --host 127.0.0.1 --port 5173
curl -i http://127.0.0.1:5173/
curl -i http://127.0.0.1:5173/api/auth/me
curl -i -X POST http://127.0.0.1:5173/api/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"123456"}'
```

### 页面与交互验证

- 浏览器打开 `/login`
- 使用 `admin / 123456` 登录
- 使用 `sales_admin / 123456` 登录
- 验证关键路由跳转
- 验证菜单可见性与角色匹配

### 通过项

- 前端依赖状态正常。
- `npm run build` 构建通过。
- Vite 本地开发服务可正常启动。
- `/api/*` 代理可正确转发到后端。
- `admin` 登录后无“部门技能申请”和“申请审批”入口。
- `admin` 直访 `/admin/applications` 时会被重定向到 `/admin/resources`。
- `sales_admin` 登录后可看到“共享资源管理”和“申请审批”入口。
- `sales_admin` 直访 `/user/applications` 时会被重定向到 `/admin/resources`。

### 阻塞项

- 本次前端验证中未发现明确阻塞项。

### 风险项

#### 1. 未登录错误消息存在乱码风险

- 位置：[SecurityConfig.java](/Users/dengfulei/Desktop/codex-ai/skill-admin/backend/src/main/java/com/codex/skilladmin/security/SecurityConfig.java#L39)
- 现象：未登录访问 `/api/auth/me` 时，响应消息出现 `????`
- 当前表现：响应头出现 `charset=ISO-8859-1`，中文错误信息不可读
- 风险：虽然不阻塞主功能，但会影响异常提示展示与演示观感

## 综合结论

本次前后端全流程验证结论如下：

1. 项目主链路已基本跑通，前后端均能完成构建、启动和基础联调。
2. 后端单测、数据库迁移、登录接口均验证通过。
3. 前端构建、开发服务、代理转发、关键路由权限表现均验证通过。
4. 当前没有发现会直接导致系统无法交付的前端失败项。
5. 当前主要问题集中在两个后端细节：
   - 默认 `8080` 端口可能被本机环境占用
   - 权限异常状态码语义与未登录错误编码存在一致性风险

## 对交付的建议

- 演示或验收前，先确认 `8080` 端口未被占用，或明确使用其他端口启动后端。
- 建议修复业务异常的 HTTP 状态码映射，使 `401`、`403` 等场景与业务语义一致。
- 建议修复未登录响应的字符编码问题，确保中文提示正常显示。
- 若用于正式提交，可将本文档与 [运行时权限验证测试记录](/Users/dengfulei/Desktop/codex-ai/skill-admin/docs/运行时权限验证测试记录.md) 一并作为测试材料。

## 相关实现位置

- 后端异常处理：[GlobalExceptionHandler.java](/Users/dengfulei/Desktop/codex-ai/skill-admin/backend/src/main/java/com/codex/skilladmin/common/GlobalExceptionHandler.java)
- 后端业务异常：[BusinessException.java](/Users/dengfulei/Desktop/codex-ai/skill-admin/backend/src/main/java/com/codex/skilladmin/common/BusinessException.java)
- 未登录返回处理：[SecurityConfig.java](/Users/dengfulei/Desktop/codex-ai/skill-admin/backend/src/main/java/com/codex/skilladmin/security/SecurityConfig.java)
- 前端路由守卫：[index.ts](/Users/dengfulei/Desktop/codex-ai/skill-admin/frontend/src/router/index.ts)
- 前端认证状态：[auth.ts](/Users/dengfulei/Desktop/codex-ai/skill-admin/frontend/src/stores/auth.ts)

## 结语

本次双子代理并行验证表明，项目已经具备较完整的交付基础。后续只需补齐少量异常处理细节，并在最终演示前再次确认端口与环境状态，即可进一步提升验收稳定性。
